apiVersion: apps/v1
kind: Deployment
metadata: {name: station-core-service, namespace: chargegrid}
spec:
  replicas: 2
  selector: {matchLabels: {app: station-core-service}}
  template:
    metadata: {labels: {app: station-core-service}}
    spec:
      containers:
        - name: station-core-service
          image: ghcr.io/chargegrid/station-core-service:__IMAGE_TAG__
          ports: [{name: http, containerPort: 8084}]
          envFrom: [{configMapRef: {name: chargegrid-config}}, {secretRef: {name: chargegrid-secrets}}]
          readinessProbe: {httpGet: {path: /actuator/health, port: http}, periodSeconds: 10}
          livenessProbe: {httpGet: {path: /actuator/health, port: http}, initialDelaySeconds: 30, periodSeconds: 20}
          resources: {requests: {cpu: 100m, memory: 256Mi}, limits: {cpu: 500m, memory: 512Mi}}
---
apiVersion: v1
kind: Service
metadata: {name: station-core-service, namespace: chargegrid}
spec:
  type: ClusterIP
  selector: {app: station-core-service}
  ports: [{name: http, port: 8084, targetPort: http}]

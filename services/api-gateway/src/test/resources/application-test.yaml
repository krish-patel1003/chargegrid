spring:
  config:
    activate:
      on-profile: test
  cloud:
    gateway:
      server:
        webflux:
          routes: []
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri:
